diff --git a/Libraries/PushNotificationIOS/PushNotificationIOS.js b/Libraries/PushNotificationIOS/PushNotificationIOS.js
index 26e1dacfb47c28bf4c0c6ee795360b507e848a58..4de700dca8a64f716851c7bcb3149cbc637af0f0 100644
--- a/Libraries/PushNotificationIOS/PushNotificationIOS.js
+++ b/Libraries/PushNotificationIOS/PushNotificationIOS.js
@@ -63,12 +63,13 @@ type NativePushNotificationIOSEventDefinitions = {
   localNotificationReceived: [{...}],
 };
 
-const PushNotificationEmitter =
-  new NativeEventEmitter<NativePushNotificationIOSEventDefinitions>(
-    // T88715063: NativeEventEmitter only used this parameter on iOS. Now it uses it on all platforms, so this code was modified automatically to preserve its behavior
-    // If you want to use the native module on other platforms, please remove this condition and test its behavior
-    Platform.OS !== 'ios' ? null : NativePushNotificationManagerIOS,
-  );
+// PATCHED: Handle missing native module to prevent crash during React Refresh
+// RN 0.82 removed React-RCTPushNotification from auto-linking but JS code still exists
+const PushNotificationEmitter = NativePushNotificationManagerIOS
+  ? new NativeEventEmitter<NativePushNotificationIOSEventDefinitions>(
+      Platform.OS !== 'ios' ? null : NativePushNotificationManagerIOS,
+    )
+  : null;
 
 const _notifHandlers = new Map<string, void | EventSubscription>();
 
@@ -364,6 +365,10 @@ class PushNotificationIOS {
         type === 'localNotification',
       'PushNotificationIOS only supports `notification`, `register`, `registrationError`, and `localNotification` events',
     );
+    // PATCHED: Return early if native module not available
+    if (!PushNotificationEmitter) {
+      return;
+    }
     let listener;
     if (type === 'notification') {
       listener = PushNotificationEmitter.addListener(
diff --git a/node_modules/react-native/scripts/.packager.env b/scripts/.packager.env
new file mode 100644
index 0000000000000000000000000000000000000000..361f5fb47d34aca545859cfa4b794257949172b0
--- /dev/null
+++ b/scripts/.packager.env
@@ -0,0 +1 @@
+export RCT_METRO_PORT=8081
