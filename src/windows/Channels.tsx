import React, { Component, useState, useRef } from "react";
import { Alert,  StyleSheet, View, TouchableHighlight, Image, ScrollView } from "react-native";
import { Text } from "native-base";

export default () => {
  return (
    <Text>Test</Text>
  );
};
